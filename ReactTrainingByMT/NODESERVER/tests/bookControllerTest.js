const should = require('should');
const sinon = require('sinon');
// const mocha = require('mocha');
const bookController = require('../controllers/bookController');

describe('Book Controller Test', () => {

    describe('Post call', () => {
        it('It should not allow an empty title', () => {

        });
    }); // # End of post suite.
});